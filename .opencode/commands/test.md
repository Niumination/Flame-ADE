Run the full test suite for Flame ADE.

1. Run TypeScript tests:
   ```bash
   pnpm test
   ```

2. Run Rust tests:
   ```bash
   cd src-tauri && cargo test
   ```

3. Report all failures with details.

4. If tests pass, report success with test count.
